class KeyFrame {
    constructor(timeline) {
        this._timeline = timeline;
        this._shapesRefs = []; // each object has the 
    }

    render(context) {
        for (const shapeRef of this._shapesRefs) {
            this._timeline.shapes[]
        }
    }
}